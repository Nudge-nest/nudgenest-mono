# =============================================================================
# nudge-nest-landing (React + Vite) — environment variables template
#
# HOW TO SET UP (one-time per environment):
#   cp .env.example .env.develop     # local dev values
#   cp .env.example .env.staging     # staging values
#   cp .env.example .env.production  # production values
#
# Then fill in the values for each file.
# Switching branches auto-copies the right file to .env:
#   develop / feature / fix  →  .env.develop
#   test                     →  .env.staging
#   master                   →  .env.production
#
# Run manually: pnpm sync-env
# =============================================================================

# ── Backend API ───────────────────────────────────────────────────────────────
# Local:   http://0.0.0.0:50001/api/v1/
# Staging: https://api-staging.nudgenest.io/api/v1/
# Prod:    https://api.nudgenest.io/api/v1/
VITE_APP_BACKEND_HOST=http://0.0.0.0:50001/api/v1/

# ── Review UI public URL (used in social proof section iframe) ────────────────
# Local:   http://localhost:3001
# Staging: https://review-staging.nudgenest.io
# Prod:    https://reviews.nudgenest.io
VITE_APP_REVIEW_UI_URL=http://localhost:3001

# ── Demo merchant (social proof widget on landing page) ───────────────────────
VITE_APP_DEMO_MERCHANT_ID=
