/**
 * Shared TypeScript types and interfaces
 *
 * These types are used across backend, frontend, and Shopify app
 * to ensure type consistency throughout the monorepo.
 */

// Placeholder - will be populated with shared types
export {};
